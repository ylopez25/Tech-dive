/*
This page is going to contain all actions performed by admin
Records of exams deleted and record of exams where updates were made
This is going to just be a test to indicate if state is being managed correctly as it relates to updates applied -> state refresh -> mod-ed data
*/

export const AdminsPage = () => {
    return (
        <div>
            Admin's Page to have Deleted Exams, Update Exams records
        </div>
    )
}
